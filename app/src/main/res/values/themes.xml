<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OnePass" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OnePass" parent="Base.Theme.OnePass">
        <item name="background">@color/background_color</item>
        <item name="colorControlActivated">@android:color/transparent</item>
    </style>

    <style name="CardViewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardUseCompatPadding">true</item>
        <item name="cardPreventCornerOverlap">false</item>
        <item name="cardBackgroundColor">#3E3E3E</item>
        <item name="cardElevation">0dp</item>
    </style>

</resources>